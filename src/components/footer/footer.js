import React from 'react';
import PropTypes from 'prop-types';
import { Container, FixedContainer, Column } from './footer.css';

const Footer = () => (
  <>
    <Container>

    </Container>
    <FixedContainer>
      <Column>
        <h1>Reach Out</h1>
      </Column>
    </FixedContainer>
  </>
)

export default Footer;